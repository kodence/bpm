{
  "name": "bp-tracker",
  "productName": "BP Tracker",
  "version": "1.0.0",
  "description": "Simple daily blood pressure tracker",
  "main": "main.js",
  "author": "kode",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"BP Tracker\" --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico --ignore=\"^/dist\" --ignore=\"^/data\""
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "@electron/packager": "^18.3.0"
  }
}
